void dfs(int x, int y){
//     visited[x][y] = 1;
//     for(int i=0;i<4;i++){
//         int ny = y + dy[i];
//         int nx = x + dx[i];
//         if(ny<0 || nx<0 || ny>=n || nx>=m) continue;
//         if(arr[nx][ny] == 1 && !visited[nx][ny]){
//             dfs(nx, ny);
//         }
//     }
//     return;
// }
